name: challenge3

on: push

env:
  ARTIFACTNAME: artifact

jobs:
  actions:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        use: actions/checkout@v2
      - name: Upload artifact
        use: actions/upload@v2
        with:
          name: ${{ env.ARTIFACTNAME }}
          path: .
